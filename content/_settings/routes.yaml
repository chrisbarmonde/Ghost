routes:

collections:
  /review/:
    permalink: /review/{slug}/
    template: index
    filter: tag:review
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
